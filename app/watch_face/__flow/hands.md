# Hands Section — Flow

**About:** [description](../__about/hands.md)

## Layout

📦 Hand-pack gallery — one LARGE tile per `hand_packs()` entry, icon =
   `thumbs.art_thumbnail(pack["files"]["hours"])` at an enlarged icon
   size (the image dominates the tile; the name sits under it)

## Behaviour (pseudocode)

    ON a tile click:
        setters["hands"](name)

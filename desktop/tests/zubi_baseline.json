{
 "_comment": "THE ZUBI BASELINE RATCHET (owner approval 2026-08-09): the owner-frozen pre-existing findings (zubi-v2-findings.md), digit/hex-normalized. test_layout_audit fails ONLY on findings not in here. Entries may only be REMOVED; adding one needs the owner's explicit in-session approval (DOMY_ZUBI_REBASELINE=force).",
 "WatchFaceDialog": [
  "[WatchFaceDialog - Colors @ minimum #x#] ALG-# UNIFORM SIBLINGS (rules/GUI.md -> Zubi v#) QPushButton row in QGroupBox '-': 'Follow ring (to white)' #x#, 'White' #x#, 'Black' #x#, 'Custom color' #x# - same-kind controls in one container must share their size within #px. Let the WIDEST content decide and give every one of them that minimum (or lay them out in a grid with equal columns)",
  "[WatchFaceDialog - Hands & Bodies @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required",
  "[WatchFaceDialog - Numerals @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required",
  "[WatchFaceDialog - Pointer @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required",
  "[WatchFaceDialog - Ring @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required",
  "[WatchFaceDialog - Themes & Slots @ minimum #x#] ALG-# UNIFORM SIBLINGS (rules/GUI.md -> Zubi v#) QPushButton row in QWidget '-': 'Weekday themes' #x#, 'Complications' #x#, 'Astrology' #x#, 'Ascendant' #x#, 'Chinese zodiac' #x# - same-kind controls in one container must share their size within #px. Let the WIDEST content decide and give every one of them that minimum (or lay them out in a grid with equal columns)",
  "[WatchFaceDialog - Themes & Slots @ minimum+#% #x#] ALG-# UNIFORM SIBLINGS (rules/GUI.md -> Zubi v#) QPushButton row in QWidget '-': 'Weekday themes' #x#, 'Complications' #x#, 'Astrology' #x#, 'Ascendant' #x#, 'Chinese zodiac' #x# - same-kind controls in one container must share their size within #px. Let the WIDEST content decide and give every one of them that minimum (or lay them out in a grid with equal columns)",
  "[WatchFaceDialog - Umbra & Aura @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required"
 ],
 "SettingsDialog": [
  "[SettingsDialog - Language  ▸ @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required",
  "[SettingsDialog - Location  ▸ @ minimum #x#] ALG-# EXTREME STATE MATRIX (rules/GUI.md -> Zubi v#) QComboBox '-' at item=#: CLIPPED QGroupBox '-': has #x#, needs at least #x# - a state the user can reach must satisfy the same rules as the default one; fix the layout for the extreme, or narrow the control's range",
  "[SettingsDialog - Location  ▸ @ minimum #x#] ALG-# EXTREME STATE MATRIX (rules/GUI.md -> Zubi v#) QComboBox '-' at item=#: CLIPPED QWidget '-': has #x#, needs at least #x# - a state the user can reach must satisfy the same rules as the default one; fix the layout for the extreme, or narrow the control's range",
  "[SettingsDialog - Location  ▸ @ minimum #x#] ALG-# EXTREME STATE MATRIX (rules/GUI.md -> Zubi v#) QComboBox '-' at item=#: OVERLAP in QGroupBox: '/QListWidget' #,# #x# is drawn over '/QComboBox' #,# #x# (#x# px shared) - a state the user can reach must satisfy the same rules as the default one; fix the layout for the extreme, or narrow the control's range",
  "[SettingsDialog - Location  ▸ @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required"
 ],
 "EncyclopediaDialog": [
  "[EncyclopediaDialog - Home @ minimum #x#] ALG-# CONTRAST (rules/GUI.md -> Zubi v#) QLabel '-' '# themes · # pages': #HEX on the rendered background #HEX is #.#:#, below the #.#:# floor for this text (#px, bold) - darken the background or lighten the text until the ratio reaches #.#:# (or make the text >= #px AND bold, which lowers the floor to #.#:#)"
 ],
 "ObservatoryDialog": [
  "[ObservatoryDialog @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required"
 ],
 "ReportDialog": [
  "[ReportDialog @ minimum #x#] ALG-# ROW OCCUPANCY (rules/GUI.md -> Zubi v#): rows y=#-# px leave their right half up to #% empty while content continues BELOW them (y=# px) - reflow those rows into columns before stacking them into height (ladder step #). Heuristic: background taken as the most common edge pixel #HEX, bands of #px, # consecutive bands required"
 ],
 "ShortcutsDialog": [],
 "TimeTravelDialog": [
  "[TimeTravelDialog @ minimum #x#] CLIPPED QLineEdit 'qt_spinbox_lineedit': has #x#, needs at least #x#",
  "[TimeTravelDialog @ minimum+#% #x#] CLIPPED QLineEdit 'qt_spinbox_lineedit': has #x#, needs at least #x#"
 ],
 "LegendPopup": []
}

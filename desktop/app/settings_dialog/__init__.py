"""M6 settings window: location picker over the bundled city
database, opacity/size/color/theme pickers and the custom art
builders — one module per navigation section, composed onto the
SettingsDialog shell in dialog.py. See [settings_dialog (folder)]
(___settings_dialog.md).
"""

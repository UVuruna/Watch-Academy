"""Configuration package — the single home for every constant and tunable.

No other module in the project may contain a numeric literal that is not
a loop index or an enum value (monorepo Rule #4).
"""
